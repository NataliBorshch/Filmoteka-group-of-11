
{{#each this}}

<li class="movie-card">
  <img class="movie-card__poster" src="https://image.tmdb.org/t/p/w500{{backdrop_path}}" alt="{{original_title}}" id="{{id}}"
   data-source="https://image.tmdb.org/t/p/w500{{poster_path}}" />
  <h3 class="movie-card__title">{{title}}</h3>
  <ul class="movie-card__info">
    <li class="movie-card__genre">
        <p>Драма</p>
    </li>
    <li class="movie-card__year"><p>{{release_date}}</p></li>
    <li class="movie-card__rate vote-accent">
        {{id}}
    </li>
  </ul>
</li>

{{/each}}